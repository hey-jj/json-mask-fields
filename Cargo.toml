[package]
name = "json-fieldmask"
version = "0.1.0"
edition = "2021"
description = "select/redact subsets of JSON objects via a Google fields-style query language"
license = "MIT"
repository = "https://github.com/hey-jj/json-fieldmask"
readme = "README.md"

[dependencies]
